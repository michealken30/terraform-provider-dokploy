---
page_title: "dokploy_registry Resource - terraform-provider-dokploy"
subcategory: ""
description: |-
  Manages a Dokploy container registry.
---

# dokploy_registry (Resource)

Manages a Dokploy container registry. Registries store Docker images for deployment.

## Example Usage

```terraform
# Configure a container registry

resource "dokploy_registry" "dockerhub" {
  registry_name   = "dockerhub"
  registry_type   = "dockerhub"
  registry_url    = "https://index.docker.io/v1/"
  username        = var.dockerhub_username
  password        = var.dockerhub_token
  organization_id = var.organization_id
}

variable "dockerhub_username" {
  description = "Docker Hub username"
  type        = string
}

variable "dockerhub_token" {
  description = "Docker Hub access token"
  type        = string
  sensitive   = true
}

variable "organization_id" {
  description = "Dokploy organization ID"
  type        = string
}

output "registry_id" {
  value = dokploy_registry.dockerhub.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `organization_id` (String) The organization ID this registry belongs to.
- `password` (String, Sensitive) The password for registry authentication.
- `registry_name` (String) The name of the registry.
- `registry_type` (String) The type of registry (e.g., 'selfHosted', 'docker', 'github', 'gitlab', 'digitalocean', 'gcp', 'azure', 'ecr').
- `registry_url` (String) The URL of the registry.
- `username` (String) The username for registry authentication.

### Optional

- `image_prefix` (String) Optional prefix for images in this registry.

### Read-Only

- `id` (String) The unique identifier of the registry.

## Import

Registries can be imported using their ID:

```shell
terraform import dokploy_registry.example hij456-registry-id
```

Or using import blocks (Terraform 1.5+):

```hcl
import {
  to = dokploy_registry.example
  id = "hij456-registry-id"
}
```

### Finding Registry IDs

Use the `dokploy_registries` data source to discover registry IDs:

```hcl
data "dokploy_registries" "all" {}

output "registry_ids" {
  value = { for r in data.dokploy_registries.all.registries : r.name => r.id }
}
```

~> **Note:** Sensitive values like `password` will not be populated during import. You will need to set these values in your configuration after import.
